name: a_star_example
description: An example showing how to use the a_star_algorithm package.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  a_star_algorithm:
    path: ../
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
